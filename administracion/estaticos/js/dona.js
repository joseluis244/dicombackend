const Dona = new Chart(ctxC, {
    // The type of chart we want to create
    type: "doughnut",
    // The data for our dataset
});
const Linea = new Chart(ctxC, {
    // The type of chart we want to create
    type: "line",
    // The data for our dataset
});
export {Dona, Linea}